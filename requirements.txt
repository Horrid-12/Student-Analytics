# New FastAPI stack (Phase 3) — switched 2026-08-29.
# requirements.txt.dead (the original Streamlit pin set) was deleted 2026-09-12
# in the Phase-4 cleanup; a legacy rebuilt would rebuild the pins from the git
# history instead.
fastapi==0.141.1
authlib==1.8.0
jinja2==3.1.6
plotly==7.1.0
httpx==0.28.1
upstash-redis==1.8.0
pandas==3.0.5
openpyxl==3.1.5
xlrd==2.0.2
python-multipart==0.0.32
psycopg[binary]==3.2.10
psycopg-pool==3.2.2